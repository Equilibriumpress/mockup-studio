(function(){
const M=window.MockupV06=window.MockupV06||{},FILES=['manifest.json','background.js','bridge.js','popup.html','popup.js','README.md'];
async function fileFromData(dataUrl,name){const r=await fetch(dataUrl),blob=await r.blob();return new File([blob],name||'capture.png',{type:blob.type||'image/png'})}
async function receive(p){if(!p?.dataUrl)return;try{await addImageFiles([await fileFromData(p.dataUrl,p.name)]);setMode('editor');$('#captureStatus').textContent=`Received ${p.name||'browser capture'} from extension.`;showToast('Browser capture added')}catch(e){console.error(e);showToast('Capture handoff failed')}}
async function zip(){const b=$('#downloadExtension');b.disabled=true;b.textContent='Preparing ZIP…';try{const files=[];for(const name of FILES){const r=await fetch(`./extension/${name}`);if(!r.ok)throw Error(name);files.push({name,blob:await r.blob()})}download(await zipStore(files),'mockup-studio-capture-v0.6.zip');showToast('Extension ZIP ready')}catch(e){console.error(e);showToast('Extension ZIP failed')}finally{b.disabled=false;b.textContent='Download Chrome / Edge extension ZIP'}}
M.installCapture=function(){M.addModeTab('capture','Capture');M.panel('capture',`<div class="panel-title">Browser capture</div><div class="capture-card"><strong>Mockup Studio Capture</strong><p>Capture a viewport, full page or one visible element and send the result straight into this editor.</p></div><button class="button primary full-width section-gap" id="downloadExtension">Download Chrome / Edge extension ZIP</button><button class="button full-width" id="captureUploadFallback">Import screenshot file</button><div class="capability" id="captureStatus">Extension handoff is ready. Captures stay local between the extension and this browser.</div><div class="panel-title section-gap">Supported</div><div class="capture-list"><span>Viewport capture</span><span>Full-page stitched capture</span><span>Visible element selection</span><span>Direct editor handoff</span></div><div class="capability">Direct arbitrary URL capture is intentionally excluded from GitHub Pages. Use the extension on the loaded page instead.</div>`);$('#downloadExtension').onclick=zip;$('#captureUploadFallback').onclick=()=>imageInput.click();window.addEventListener('message',ev=>{if(ev.source===window&&ev.data?.source==='mockup-studio-extension'&&ev.data?.type==='capture')receive(ev.data.payload)})}
})();
